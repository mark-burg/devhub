node_modules/
.DS_Store
# Build output (deployed to gh-pages by .github/workflows/deploy-site.yml)
dist/
# Demo/preview content from `npm run demo` — on GitHub, reports and data live on gh-pages only
.devhub-preview/
